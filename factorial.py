# Factorial: Calculate the factorial of a non-negative integer.
from math import factorial

def computefactorial():
  num = int(input("Enter an integer: "))
  print(f"The factoral is {factorial(num)}")

computefactorial()
